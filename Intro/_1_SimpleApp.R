# Create a simple APP.
# Just Display Text in Title panel, Sidebar Panel, Main Panel.

# create Ui.R and server.R
# Save file and Run the APP..

# install.packages("shiny", repos=c("http://rstudio.org/_packages", "http://cran.rstudio.com"))

library(shiny)

# ui <- fluidPage(
#   
# )

# server <- function(input, output, session) {
#   
# }

shinyApp(ui, server)

